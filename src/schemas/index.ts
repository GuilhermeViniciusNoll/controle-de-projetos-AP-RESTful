import validEmailSchema from "./validEmail.schema"
import validNameSchema from "./validName.schema"
import validIdSchema from "./validId.schema"

export default { validEmailSchema, validNameSchema, validIdSchema }